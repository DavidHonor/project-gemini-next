import React, { useContext } from "react";
import { RocketContext } from "./RocketContext";

const RocketStages = () => {
    const {} = useContext(RocketContext);

    return <div>RocketStages</div>;
};

export default RocketStages;
